## Output Formats

### Default Outputs (Always Generated)
- HTML
- JSON

### Optional Outputs (User Requested)
- Excel
- PDF
- XML

Optional outputs are controlled via the `additional_outputs`
field in the JSON instruction file stored in S3.
